import styled from "styled-components"
import { Icon } from "antd"
import React from "react"

const LoadingIcon = styled(Icon)`fontSize: 64;`

export default () => <LoadingIcon type="loading" style={{ fontSize: 64 }} />
